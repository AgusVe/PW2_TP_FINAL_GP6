{{> header_logueado}}
<div class="container">
    <div class="lobby-container">
        {{#usuarios}}
            <h1 class="nombreUsuario">{{usuario}} </h1>
            <div>
                <h1 class="puntaje">Puntaje Total: {{puntosTotales}}</h1>
            </div>
            <div class="btn-container">
                <div id="mapa"></div>
                <img src='/uploads/qrcode_{{usuario}}.png' id="qr" alt="Código QR">
            </div>
        {{/usuarios}}
        <h2>Partidas Jugadas</h2>
        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th scope="col">Partida</th>
                <th scope="col">Puntos</th>
            </tr>
            </thead>
            <tbody>
            {{#userPartidas}}
                <tr>
                    <td>Partida {{numPartidaDelJugador}}</td>
                    <td class="win">{{puntosObtenidos}}</td>
                </tr>
            {{/userPartidas}}
            </tbody>
        </table>
    </div>
</div>
{{> footer}}
