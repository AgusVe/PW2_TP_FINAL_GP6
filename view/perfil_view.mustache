{{> header_logueado}}

{{#usuarios}}
<body onload="loadMap({{latitud}},{{longitud}})" >
{{/usuarios}}
<div class="container">
    <div class="lobby-container">
        {{#usuarios}}
            <h1 class="nombreUsuario">{{usuario}} </h1>
            <div>
                <h1 class="puntaje">Puntaje Total: {{puntosTotales}}</h1>
            </div>
            <div class="btn-container row">
                <div class="col-8" id="mapa" style="width:250px; height:200px;"></div>
                <img class="col-4" src='/uploads/qrcode_{{usuario}}.png' alt="Código QR">
            </div>
        {{/usuarios}}
        <h2>Partidas Jugadas</h2>
        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th scope="col">Partida</th>
                <th scope="col">Puntos</th>
            </tr>
            </thead>
            <tbody>
            {{#userPartidas}}
                <tr>
                    <td>Partida {{numPartidaDelJugador}}</td>
                    <td class="win">{{puntosObtenidos}}</td>
                </tr>
            {{/userPartidas}}
            </tbody>
        </table>
    </div>
</div>
{{> footer}}
